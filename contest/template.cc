#include "bits/stdc++.h"
using namespace std;

#define all(x) (x).begin(), (x).end()
#define len(x) ((int)(x).size())

using ll = int64_t;
const char nl = '\n';

int main() {
  ios_base::sync_with_stdio(0), cin.tie(NULL);

  return 0;
}